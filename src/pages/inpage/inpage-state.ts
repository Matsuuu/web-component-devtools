import { TreeElement } from "../content/lib/element";

export const inpageState = {
    selectedElement: undefined as TreeElement | undefined,
    previousContextMenuTarget: undefined as Element | undefined,
};
